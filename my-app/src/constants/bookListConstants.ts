export enum boookSortingFilters {
  RELEVANCE = "relevance",
  NEWEST = "newest",
}

export enum bookCategories {
  ALL = "all",
  ART = "art",
  BOIGRAPHY = "biography",
  COMPUTERS = "computers",
  HISTORY = "history",
  MEDICAL = "medical",
  POETRY = "poetry",
}

export const BOOKS_PER_FETCH = 30;
